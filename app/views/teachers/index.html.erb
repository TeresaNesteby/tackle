<% if teacher_signed_in? %>
  Signed in as <%= current_teacher.first_name %>
  <%= current_teacher.last_name %>

  <%= link_to "Edit Profile", edit_teacher_path(current_teacher)  %>
  <%= link_to "Signout", destroy_teacher_session_path, :method => :delete %>
<% end %>

